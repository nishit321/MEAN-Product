import {registerDecorator, ValidationOptions, Validator} from "class-validator";

// own validation decorator to check if string is a valid password
export function IsValidPassword(validationOptions?: ValidationOptions) {
  /*  TODO: to be implemented
   *    at least one upper and one lower case
   *    minimum of 8 characters
   *    at least one special character
   *    not allowed to be similar to the registration name or pseudonym
   */

}
